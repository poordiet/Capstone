  
<p id="notice"><%= notice %></p>

<div class="container">
  <br>
  <h1>Service Statuses</h1>
  <br>
  <a class="btn btn-success float-right mt-4" href="service_statuses/new" role="button" aria-pressed="true">New Status</a>
  <table id="example" class="table table-hover table-bordered">
    <thead class="thead-dark">
      <th>Status Name</th>
      <th>Show</th>
      <th>Edit</th>
    </thead>

    <tbody>
      <% @service_statuses.each do |ss| %>
        <tr>
          <td><%= ss.service_status %></td>
          <td><%= link_to 'Show', ss, class: 'btn btn-info btn-block' %></td>
          <td><%= link_to 'Edit', edit_service_status_path(ss), class: 'btn btn-warning btn-block' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<br>
<br>

<script type="text/javascript">
    var dataTable = $('#example').DataTable();

    $(document).ready(function() {
        dataTable;
    });

    document.addEventListener("turbolinks:before-cache", function() {
        if (dataTable !== null) {
            dataTable.destroy();
            dataTable = null;
        }
    });
</script>