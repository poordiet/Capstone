<%= form_with(model: style, local: true) do |form| %>
  
<div id="form">
  <br>
  <% if style.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(style.errors.count, "error") %> prohibited this style from being saved:</h4>

      <ul>
        <% style.errors.full_messages.each do |message| %>
          <li>• <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <h1>Edit Style</h1>
  <br>
  <div class="form-row">
    <div class="field form-group col-md-2">
      <%= form.label :style_name, 'Style Name *' %> 
      <%= form.text_field :style_name, class: 'form-control' %>
    </div>
  </div>

  <div class="form-row">
    <div class="field form-group col-md-4">
      <%= form.label :style_description, 'Style Description' %>
      <%= form.text_area :style_description, class: 'form-control' %>
    </div>
  </div>

<div class="form-row">
  <div class="field form-group col-md-8">
    <div id="buttons">
      <%= link_to 'Cancel', :back, class: 'btn btn-danger btn-lg float-left'%>
      <button type="submit" class="btn btn-primary btn-lg float-right ">Submit</button>
    </div>
  </div>
</div>
<% end %>