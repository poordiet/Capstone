<%= form_with(model: style, local: true) do |form| %>
  <% if style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(style.errors.count, "error") %> prohibited this style from being saved:</h2>

      <ul>
        <% style.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :style_name %>
    <%= form.text_field :style_name %>
  </div>

  <div class="field">
    <%= form.label :style_description %>
    <%= form.text_field :style_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
