json.extract! grooming_appointment, :id, :pet_id, :store_id, :payment_type_id, :appt_date, :appt_blades, :appt_total, :appt_total, :appt_notes, :customer_feedback, :vac_current, :vac_current_date, :created_at, :updated_at
json.url grooming_appointment_url(grooming_appointment, format: :json)
