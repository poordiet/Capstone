<%= form_with(model: incident_status, local: true) do |form| %>
 
<div class="status-content">
<br>
 <% if incident_status.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(incident_status.errors.count, "error") %> prohibited this incident status from being saved:</h4>

      <ul>
        <% incident_status.errors.full_messages.each do |message| %>
          <li>• <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <h1>New Incident Status</h1>
  <i>* Indicates Required Fields</i>
  <hr>
  <div class="form-row">
  <div class ="field form-group col-md-6">
    <%= form.label :status, 'Incident Status *' %>
    <%= form.text_field :status, class: 'form-control' %>
  </div>
 </div>
   <div class="form-row">
  <div class ="field form-group col-md-12">
    <%= form.label :definition, 'Status Defintion' %>
    <%= form.text_area :definition, class: 'form-control', rows: '3' %>
  </div>
  </div>

  <div class="form-row">
  <div class="field form-group col-md-12">
      <%= link_to 'Cancel', incident_statuses_path, class: 'btn btn-danger btn-lg float-left'%>
      <button type="submit" class="btn btn-primary btn-lg float-right ">Submit</button>
  </div>
</div>
<% end %>
