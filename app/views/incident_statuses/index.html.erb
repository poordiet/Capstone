<p id="notice"><%= notice %></p>

<div class="container">
  <br>
  <h1>Incident Statuses</h1>
  <br>
  <a class="btn btn-success float-right mt-4" href="incident_statuses/new" role="button" aria-pressed="true">New Status</a>
  <table id="example" class="table table-hover table-bordered">
    <thead class="thead-dark">
      <th>Status Name</th>
      <th>Status Defintion</th>
      <th>Show</th>
      <th>Edit</th>
    </thead>

    <tbody>
      <% @incident_statuses.each do |is| %>
        <tr>
          <td><%= is.status %></td>
          <td><%= is.definition %></td>
          <td><%= link_to 'Show', is, class: 'btn btn-primary btn-block' %></td>
          <td><%= link_to 'Edit', edit_incident_status_path(is), class: 'btn btn-warning btn-block' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<br>
<br>

<script type="text/javascript">
    var dataTable = $('#example').DataTable();

    $(document).ready(function() {
        dataTable;
    });

    document.addEventListener("turbolinks:before-cache", function() {
        if (dataTable !== null) {
            dataTable.destroy();
            dataTable = null;
        }
    });
</script>
